using Gtk 4.0;

template $TermplexDebugWarning: Gtk.Box {
  orientation: vertical;

  Gtk.Label {
    label: _("⚠️ You're running a debug build of Termplex! Performance will be degraded.");
    margin-top: 10;
    margin-bottom: 10;
  }
}

// This makes Termplex available to the XCode build for the macOS app.
// We append "Kit" to it not to be cute, but because targets have to have
// unique names and we use Termplex for other things.
module TermplexKit {
    umbrella header "termplex.h"
    export *
}
